# 🔷 NONEcore Bot

ربات تلگرام اهدای خودکار کانفیگ VPN با استخراج از فایل HTML اکسپورت شده از کانال‌های تلگرام.

## ✨ ویژگی‌ها

- 📤 استخراج خودکار کانفیگ از فایل HTML
- 🎲 ارسال رندوم کانفیگ‌ها
- 📊 محدودیت روزانه قابل تنظیم
- ⛔ توقف اضطراری ارسال
- 📈 آمار کامل کانفیگ‌ها و کاربران
- 🔴 سیستم گزارش خرابی و حذف هوشمند
- ⚙️ تنظیمات کامل از طریق منو
- 🐳 پشتیبانی از Docker

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.11+
- Docker (اختیاری)
- سرور با دسترسی به اینترنت (برای fps.ms رایگان کافی است)

### نصب دستی

1. **کلون پروژه:**
```bash
git clone &lt;repository-url&gt;
cd NONEcore-bot


نصب وابستگی‌ها:
bash

pip install -r requirements.txt
تنظیمات:
bash

cp .env.example .env
# فایل .env را ویرایش کنید و مقادیر را پر کنید
اجرا:
bash

python main.py
نصب با Docker
bash

docker build -t nonecore-bot .
docker run -d --name nonecore-bot --env-file .env nonecore-bot
نصب در fps.ms
فایل‌های پروژه را در یک پوشه قرار دهید
فایل .env را با مقادیر صحیح پر کنید
پوشه را به صورت zip فشرده کنید
در پنل fps.ms آپلود کنید
پورت را روی 8080 تنظیم کنید
متغیرهای محیطی را در بخش Environment Variables وارد کنید
ربات را استارت کنید
⚙️ تنظیمات (.env)


متغیر	توضیحات	مثال
BOT_TOKEN	توکن ربات از @BotFather	123456789:ABC...
ADMIN_ID	آیدی عددی ادمین از @userinfobot	123456789
CHANNELS	کانال مقصد (با کاما جدا شود)	@nonecorebot
BATCH_SIZE	تعداد هر batch	5
BATCH_INTERVAL	فاصله بین batch‌ها (ثانیه)	120
DAILY_LIMIT	محدودیت روزانه	200
BRAND_NAME	نام برند	NONEcore
📖 راهنمای استفاده
منوی اصلی
📤 آپلود HTML - آپلود فایل HTML اکسپورت شده از کانال
📊 آمار - مشاهده آمار کامل
📤 ارسال دستی - ارسال سریع یا دستی کانفیگ‌های در صف
📱 کلاینت‌ها - دریافت لینک کلاینت‌های پیشنهادی
⚙️ تنظیمات - تغییر تنظیمات ربات
🔔 یادآوری - تنظیم یادآوری تمدید
🔄 استارت مجدد - راه‌اندازی مجدد ربات
⛔ توقف ارسال - توقف فوری ارسال
تنظیمات قابل تغییر
⏱️ فاصله ارسال (interval)
🔢 تعداد هر batch
⏳ تأخیر باقیمانده (delay)
✅/❌ ارسال کلاینت‌ها
✅/❌ یادآوری renewal
📊 محدودیت روزانه
📢 مدیریت کانال‌ها
فرآیند کار
فایل HTML از کانال تلگرام اکسپورت کنید
از منوی "📤 آپلود HTML" فایل را ارسال کنید
کانفیگ‌ها استخراج و به صف اضافه می‌شوند
از "📤 ارسال دستی" برای ارسال استفاده کنید:
⚡ ارسال سریع ۱۰ تایی
📝 ارسال دستی با تعیین تعداد
🗄️ ساختار دیتابیس
جدول configs
اطلاعات کانفیگ‌های استخراج شده
وضعیت ارسال و آمار استفاده
جدول settings
تنظیمات ربات
وضعیت توقف/ارسال
جدول daily_stats
آمار روزانه ارسال
تعداد کاربران و گزارش‌ها
جدول channels
لیست کانال‌های مقصد
🔧 عیب‌یابی
ربات استارت نمی‌شود
بررسی کنید BOT_TOKEN صحیح باشد
ADMIN_ID باید عددی باشد (نه username)
کانفیگ ارسال نمی‌شود
بررسی کنید ربات در کانال ادمین باشد
بررسی کنید CHANNELS با @ شروع شود
خطای دیتابیس
مطمئن شوید پوشه data/ قابل نوشتن است
در Docker مسیر /app/data باید volume باشد
📝 نکات مهم
فایل HTML باید از کانال تلگرام اکسپورت شده باشد
حداکثر حجم فایل: 10 مگابایت
کانفیگ‌ها به صورت رندوم ارسال می‌شوند
اگر محدودیت روزانه برسد، بقیه به فردا موکول می‌شود
کانفیگ با ۵ گزارش خرابی حذف می‌شود
🆘 پشتیبانی
در صورت مشکل، لاگ‌ها را بررسی کنید:


# بدون Docker
python main.py

# با Docker
docker logs nonecore-bot
📄 لایسنس
این پروژه متن‌باز است و تحت لایسنس MIT منتشر شده است.


---

## چک‌لیست نهایی فایل‌ها (11 فایل)
NONEcore-bot/
├── main.py              ✅
├── config.py            ✅
├── database.py          ✅
├── processor.py         ✅
├── sender.py            ✅
├── keyboard.py          ✅
├── requirements.txt     ✅
├── Dockerfile           ✅
├── .env.example         ✅ (کامل شد)
└── README.md            ✅ (اضافه شد)
